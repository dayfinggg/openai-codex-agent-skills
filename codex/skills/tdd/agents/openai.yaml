interface:
  display_name: "TDD"
  short_description: "Develop through a focused red green loop"
  default_prompt: "Use $tdd to implement this change through a focused red green loop."

policy:
  allow_implicit_invocation: true
