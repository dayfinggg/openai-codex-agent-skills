interface:
  display_name: "Review Changes"
  short_description: "Find evidence-backed defects in concrete changes"
  default_prompt: "Use $review-changes to inspect this concrete change and report only introduced, actionable defects."
