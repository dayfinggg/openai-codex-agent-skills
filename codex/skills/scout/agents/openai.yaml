interface:
  display_name: "Scout"
  short_description: "Map an unfamiliar codebase without changing it"
  default_prompt: "Use $scout to map this unfamiliar codebase and trace the relevant flow."

policy:
  allow_implicit_invocation: true
