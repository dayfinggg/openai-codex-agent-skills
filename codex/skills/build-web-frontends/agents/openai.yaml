interface:
  display_name: "Build Web Frontends"
  short_description: "Build accessible frontends in existing stacks"
  default_prompt: "Use $build-web-frontends to implement this browser UI in the existing stack while preserving the supplied design and design system."
